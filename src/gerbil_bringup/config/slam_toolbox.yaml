slam_toolbox:
  ros__parameters:
    # Plugin params
    solver_plugin: solver_plugins::CeresSolver
    ceres_linear_solver: SPARSE_NORMAL_CHOLESKY
    ceres_preconditioner: SCHUR_JACOBI
    ceres_trust_strategy: LEVENBERG_MARQUARDT
    ceres_dogleg_type: TRADITIONAL_DOGLEG
    ceres_loss_function: None

    # ROS Parameters
    odom_frame: odom
    map_frame: map
    base_frame: base_footprint
    scan_topic: /scan
    use_map_saver: true
    mode: mapping

    # Process near changes only (good for slow robots)
    map_update_interval: 3.0          # update map more frequently
    resolution: 0.05
    max_laser_range: 5.0               # ZED depth is unreliable past ~5m
    minimum_time_interval: 0.5
    transform_timeout: 0.5             # more tolerance for ZED VO latency
    tf_buffer_duration: 30.0

    # Map size constraints (metres) — ~5yd x 5yd space + margin
    # Map will be centred on the robot's start position
    map_size_x: 8.0
    map_size_y: 8.0

    # Throttle (Jetson Nano friendly)
    throttle_scans: 1
    enable_interactive_mode: true

    # Correlation search (how far to search for loop closures)
    use_scan_matching: true
    use_scan_barycenter: true
    minimum_travel_distance: 0.15      # denser scans for narrow FOV depth camera
    minimum_travel_heading: 0.2        # capture more heading changes with limited FOV

    # Correlation search parameters
    correlation_search_space_dimension: 0.5
    correlation_search_space_resolution: 0.01
    correlation_search_space_smear_deviation: 0.1

    # Loop closure parameters
    loop_search_space_dimension: 4.0   # smaller search = fewer false loop closures
    loop_search_space_resolution: 0.05
    loop_search_space_smear_deviation: 0.03
    loop_search_maximum_variance_coarse: 3.0

    # Fine search
    distance_variance_penalty: 0.5
    angle_variance_penalty: 1.0
    fine_search_angle_offset: 0.00349
    coarse_search_angle_offset: 0.349
    coarse_angle_resolution: 0.0349

    # Stack size for mapper
    stack_size_to_use: 40000000